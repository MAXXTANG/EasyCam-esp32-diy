{
  "name": "easycam-notify",
  "version": "1.0.0",
  "description": "EasyCam motion monitor - Cloudflare Worker relay",
  "main": "src/worker.js",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy"
  },
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
